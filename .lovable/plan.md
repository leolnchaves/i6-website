Em `src/components/hometeste/TestemunhosCompact.tsx`, atualizar o copy do CTA:

- PT: `"Veja os resultados que esses clientes têm recuperado"` → `"Ver histórias de sucesso"`
- EN: `"See the results these clients have been recovering"` → `"See success stories"`

O destino `localized('/success-stories')` já está correto — sem mudanças no `to`.

Arquivos afetados: `src/components/hometeste/TestemunhosCompact.tsx`.
