Inverter os três indicadores da seção "Seus dados geram vantagem competitiva..." na Home (componente `src/components/hometeste/TeseSection.tsx`) para enfatizar a maioria que NÃO faz, em vez da minoria que faz.

## Mudanças

Atualizar o array `stats` em PT e EN:

**PT**
- `89%` — das empresas não monetizam seus dados (não geram receita a partir deles)
- `69%` — das empresas não usam dados para decisões estratégicas
- `89%` — das empresas não personalizam experiências baseadas em comportamento

**EN**
- `89%` — of companies don't monetize their data
- `69%` — of companies don't use data for strategic decisions
- `89%` — of companies don't personalize experiences based on behavior

## Fora de escopo
- Sem mudanças visuais (cores, tamanhos, animação de contagem permanecem).
- Sem alterações em outros textos da seção (pergunta, narrativa, bullets, bridge).
- Sem mudanças em outras seções/páginas.

Confirme os textos exatos ou ajuste antes de eu implementar.