# Substituir placeholder do mural da Comunidade

Substituir o placeholder atual do quarto recorte do mural de arquétipos da página `/comunidade` pela foto anexa (pessoa ao lado de um quadro branco de vidro com anotações matemáticas).

A troca preserva exatamente o recorte, posicionamento, rotação e responsabilidade já definidos no componente `CommunityMural.tsx`. Apenas o `src` do item correspondente em `MURAL_IMAGES` muda.

Ao final, build, checagem de tipos e verificação visual em PT, EN, ES, desktop e mobile.