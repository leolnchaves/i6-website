# Renomear rota /comunidade para /community

Tornar todos os slugs de URL em inglês, conforme solicitado: `/i6-builders`, `/community`, `/docs`.

A única mudança necessária é renomear a rota atual `/comunidade` para `/community` em todos os pontos de navegação, sitemap e redirecionamentos. Componentes, dados e imagens internos permanecem com o nome `comunidade` (pasta e identificadores), pois isso não afeta a URL.

Ao final, build, checagem de tipos e verificação visual em PT, EN, ES, desktop e mobile.